import React from "react";

import AppContainer from "../AppContainer";
import AppRoutes from "../AppRoutes";

const Root = () => (
  <AppContainer>
    <AppRoutes />
  </AppContainer>
);

export default Root;
